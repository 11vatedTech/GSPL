{
  "name": "@gspl/provenance-checker",
  "version": "0.1.0",
  "description": "GSPL Provenance Checker — validates canonical decisions and inventions against required evidence references. Fails on malformed canon data.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "gspl-provenance": "dist/bin.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "license": "MIT",
  "author": "11vatedTech <11vated11@gmail.com>",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
